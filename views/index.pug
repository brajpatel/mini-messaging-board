extends layout

block content
  div.container
    h1= title
    ul
      each message in messages
        li
          p= message.user + ' - ' + message.added
          p= message.text
  a(href="/new" class="form-link")= 'Create New Message >'